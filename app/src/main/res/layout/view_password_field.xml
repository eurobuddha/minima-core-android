<?xml version="1.0" encoding="utf-8"?>
<!-- A password box with a reveal toggle.
     Typing a password you cannot see is how a wrong character gets committed, and for the
     passwords in this app a wrong character is not recoverable. Inflated wherever one is needed
     so every password field in Backup & Recovery behaves the same way. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <EditText
        android:id="@+id/password_input"
        style="@style/FormField"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:inputType="textPassword"
        android:importantForAutofill="no" />

    <ImageButton
        android:id="@+id/password_toggle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="6dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_visibility"
        android:contentDescription="Show password" />
</LinearLayout>
