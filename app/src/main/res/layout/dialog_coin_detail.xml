<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/core_surface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingHorizontal="20dp"
        android:paddingTop="4dp"
        android:paddingBottom="16dp">

        <!-- one view_coin_detail_row per non-empty field, built in CoinDetailDialog -->
        <LinearLayout
            android:id="@+id/coin_detail_rows"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="6dp"
            android:text="Raw JSON"
            android:textAppearance="@style/TextAppearance.MinimaCore.Eyebrow" />

        <TextView
            android:id="@+id/coin_detail_json"
            style="@style/MonoBlock"
            android:background="@drawable/bg_token_tile"
            android:padding="12dp"
            android:textSize="11.5sp"
            android:textIsSelectable="true" />
    </LinearLayout>
</ScrollView>
